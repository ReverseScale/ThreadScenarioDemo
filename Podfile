# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CGDDemoSwift' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
pod "AsyncSwift"

  # Pods for CGDDemoSwift

  target 'CGDDemoSwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CGDDemoSwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
